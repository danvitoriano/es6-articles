;
(function() {
  // 05 - var let const

  console.log("Use `var` for top-level variables that are shared across many (especially larger) scopes. ");
  console.log("Use `let` for localized variables in smaller scopes.");
  console.log("Refactor `let` to `const` only after some code has to be written, and you're reasonably sure that you've got a case where there shouldn't be variable reassignment.")


})();
